version: "3.8"

services:
  db:
    image: postgres:13.0-alpine
    env_file: ./.env
  backend:
    build: .
    command: python manage.py runserver 0.0.0.0:8001
    volumes:
      - .:/code
    ports:
      - "8001:8001"
    depends_on:
      - db
    env_file: .env
